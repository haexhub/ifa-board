<!-- spaex-composed:source_hash="84149851efb7b2369c56f82caa6d8db3dffe54d3d28a8fac1f24b65bf57836e0" build_input_hash="af2980b398313f9fd776b0fbdde915f784cc47a246cc19e3c830d50e891cc668" version="1" -->
# spaex Behavior Harness

_This file is generated by `spaex install`. Do not edit by hand._
_Change fragments in `.spaex/constitution.d/` and re-run install._

## MUST

- A hand-maintained source file that is clearly over 500 lines of code must be split, or have an explicit, documented reason to remain large together with a concrete plan for a later split. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- All changes in an existing Git repository must be authored in a dedicated repository-local worktree on a topic branch, applying to source code, tests, documentation, configuration, and generated files that are part of the change. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test bodies and test-only helpers must live in dedicated test files, even when a language-required test-module declaration or registration remains in the production file. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Test code must be maintained in files separate from production code in every language, covering unit, integration, contract, and end-to-end tests. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Before authoring any new named function, class, method, component, store, hook, service, module, CLI command, or public API endpoint, consult the project's `graphify` knowledge graph using plain CLI invocations (`graphify query "<intent>"`, `graphify path <from> <to>`, `graphify explain <symbol>`, never a harness-specific slash-command substitute) and evaluate every candidate returned, including artifacts that are unexported, incomplete, or unreachable from public surfaces, preferring to extend an existing candidate over authoring a parallel implementation; this does not apply to renames, in-place edits, trivial inline expressions, or code being deleted, and while duplicated test arrange/assert/setup blocks are exempt, helper functions inside test files remain in scope. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- On a tracked branch, run `graphify update <repo-root>` to build the graph before continuing when `graphify-out/` or its `graph.json` is absent, and to refresh it when `graphify-out/.meta.json` is absent, invalid, or its `indexed_at_sha` does not match current `HEAD`; this holds even when the `post-commit`/`post-checkout` git hooks are not installed or were bypassed (e.g. `git commit --no-verify`). _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Treat a new function name close to an existing one, the third date/retry/validation/error-handling utility with slightly different semantics, ten or more lines copied "just to adapt slightly", or the thought "similar but different enough" as a signal to stop and run the consult step before proceeding. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- When a candidate is identical or near-identical to what would be authored, propose extending it using the Refactor Proposal Format (candidate location, proposed signature, estimated lines saved, and one concrete call-site rewritten as proof-of-concept) rather than silently duplicating it, and wait for the operator to approve, redirect, or ask for more detail before making further changes; when the match is borderline, or extending would risk scope creep, stop and ask the operator rather than deciding autonomously. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Before writing new code, climb the laziness ladder in order: check whether it needs to be built at all (YAGNI), reuse an existing helper, util, or pattern already in the codebase, use the standard library, use a native platform feature, use an already-installed dependency, write it as one line if possible, and only then write the minimum code that works. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Fix bugs at the root cause: grep every caller of the touched function and fix the shared function once rather than patching only the path the ticket names. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Leave one runnable check behind for every non-trivial logic change, using the project's existing test framework and fixtures, or a framework-free assert-based demo or small test file only when the project has none yet; trivial one-liners need no test. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Mark deliberate simplifications that cut a real corner with a known ceiling, such as a global lock, an O(n²) scan, or a naive heuristic, with a `ponytail:` comment naming the ceiling and the upgrade path. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Understand the problem before climbing the laziness ladder: read the task and the code it touches and trace the real flow end to end first. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## MUST_NOT

- LLM agents must not leave references to themselves, their model, runtime, role, prompts, or participation in project artifacts, including commit messages, commit trailers such as `Co-authored-by`, source and documentation comments, changelogs, generated content, and metadata. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- This context-map operation must not cause a full graph or full repository dump to be copied into the agent context; do not compensate for a truncated or ambiguous result by dumping the repository, since it is a navigation aid rather than a replacement for reading the source of a selected artifact or for the required graphify-first-authoring graph consultation. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- A failure to bootstrap or refresh the graph, or a `graphify query`/`path`/`explain` invocation that errors, times out, or returns garbage, must not block authoring: warn in the response, naming the failed invocation and outcome for a query failure, proceed with the consultation/authoring flow, and flag the skipped or incomplete step for a later manual review. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- On a linked worktree or a feature branch, never refresh the graph snapshot or compare its freshness marker against the branch's advancing `HEAD`; use the complete fork-point snapshot as-is, warning and continuing per the failed-consultation handling if it lacks `graph.json`. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Do not add a new dependency when it can be avoided. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not add boilerplate that nobody asked for. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Do not introduce speculative abstractions, or abstractions added solely because a change might be reusable. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Never let laziness skip input validation at trust boundaries, error handling that prevents data loss, security, accessibility, the calibration real hardware needs, or anything explicitly requested. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD

- A file should have one primary reason to change, and should be split when it has unrelated responsibilities, independent public concepts, different change owners, or tests that are difficult to isolate. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- Comments should explain why a non-obvious decision exists, what invariant must be preserved, or which external constraint applies. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- For any question about the codebase, such as what something does, where it lives, how pieces relate, whether an existing artifact already satisfies a request, or how to trace a symptom back to its source, including exploratory lookups incidental to a larger task, run a bounded `graphify query "<intent>" --budget <N>` (or `graphify path`/`graphify explain` for one specific relationship or symbol) before a raw `grep`/`rg` search or broad file reads, starting around an 800 to 1500 token budget and increasing it only if needed; extract the cited locations, symbols, and relationships from the result and read only the smallest source slices needed, narrowing the question or making one additional focused query if the result is truncated or ambiguous — the only exemption is a local one-file edit at a symbol whose location the agent already has from earlier in the same task. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-context-map`]_
- Follow the existing runtime and module-system conventions rather than assuming a platform-specific global or API exists everywhere. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Keep asynchronous control flow explicit: `await` promises whose result or failure matters, handle errors at the boundary that can recover or report them, and use `Promise.all` only when operations are independent and fail-fast behavior is acceptable. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Keep modules and exports focused, preferring named exports for reusable library code and avoiding circular dependencies. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Prefer `const` and narrow local mutation, reserving `let` for reassignment that makes a state transition clearer. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Prefer immutable transformations at shared boundaries, but allow simple local mutation when it is clearer and measurably avoids needless allocation. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Preserve error causes when translating errors, adding context at a boundary rather than catching an error merely to log and rethrow it unchanged. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Treat TypeScript's `strict` mode as the baseline for new or actively maintained TypeScript code, and keep any necessary untyped escape hatch local with an explanation of why it is necessary. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Use `===`/`!==`, `??`, and `?.` deliberately, deciding explicitly how the absence of a required value is handled. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Use the project's configured formatter, linter, compiler, and test runner; at minimum, TypeScript changes should pass `tsc --noEmit` (or the project's equivalent), linting, formatting checks, and the relevant tests. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Validate data once at trust boundaries (HTTP responses, request bodies, files, environment variables, message queues, browser storage) and pass the resulting domain type inward, since a type assertion has no runtime effect and does not make the data safe. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Await every promise that affects the assertion, using `Promise.all` or `Promise.allSettled` when the test intentionally exercises concurrent work and its failure semantics. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Choose the narrowest test level that proves the behavior: unit tests for pure logic, integration tests for module boundaries and persistence, and end-to-end tests for critical user journeys. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Keep mocks narrow and behavior-oriented, mocking an external boundary only when isolation or determinism requires it. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Keep tests deterministic and isolated, controlling time, randomness, environment, and external clients at explicit boundaries. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Keep type-level tests separate from runtime tests when the project needs to verify inference, assignability, or declaration output. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Prefer a small number of clear assertions per scenario, with test names describing the behavior and relevant condition rather than the implementation method name alone. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Test both successful and failure behavior, including malformed input, missing values, rejected promises, cancellation or timeout behavior, and authorization or persistence failures where they matter. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Test observable behavior and public contracts rather than the exact arrangement of private functions or implementation details, so that a refactor preserving the contract does not require rewriting the test suite. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Use the repository's configured runner, environment, formatter, linter, and compiler, including relevant tests that pass the project's normal test command. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Prefer deletion over addition, boring solutions over clever ones, and the fewest files possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Prefer the shortest working diff, but only once the problem is understood; the smallest change in the wrong place is a second bug, not laziness. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- Question complex requests by asking whether a simpler existing option already covers the need. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
- When two standard library approaches are the same size, pick the edge-case-correct one rather than the flimsier algorithm. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_

## SHOULD_NOT

- Should not introduce a barrel export or a new abstraction only to shorten an import path. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not leave a promise unhandled. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not replace the repository's configured tooling with a personal global installation. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not use `any`; prefer a precise type, a generic, or `unknown` followed by explicit narrowing. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not use `||` when a valid value such as `0`, `false`, or `""` must be preserved, and should not hide a required value behind optional chaining without deciding how its absence is handled. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not use mutation or shared mutable module state as an accidental communication channel. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Should not assume a test file is discovered unless the runner configuration confirms it. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Should not leave a floating promise in a test, and should not let a test pass before the asynchronous work it is meant to verify has completed. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Should not mock a unit's own logic so thoroughly that the test only verifies the mock wiring. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Should not rely on real network calls, shared mutable state, random values without a controlled seed, or arbitrary timer sleeps. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_
- Should not turn every test into a slow end-to-end test. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript-testing`]_

## MAY

- Generated files, vendored code, snapshots, and other files not normally edited by hand may exceed the 500-line-of-code boundary, though they should be excluded from that size assessment. _[from `com.github.haexmas.atoms.general-coding/general-coding`]_
- The operator may suspend this authoring rule for the current session with any natural-language request to that effect, such as "skip graphify check" or "don't consult the graph this session"; the suspension applies only to that session, must be re-issued in a new session to apply again, and does not exempt the agent from noting in its response which artifacts it would have flagged had the rule been active. _[from `com.github.haexmas.atoms.graphify-first-authoring/graphify-first-authoring`]_
- Existing TypeScript projects may opt out of individual strict-mode checks during migration, provided that choice is made explicit. _[from `com.github.haexmas.atoms.javascript-typescript/javascript-typescript`]_
- Introduce a concrete, domain-owned abstraction when code evidence shows it improves cohesion, removes real duplication, or makes a branch independently testable, even when the task did not name the extraction, as long as it is kept as small as possible. _[from `com.github.haexmas.atoms.ponytail/ponytail`]_
